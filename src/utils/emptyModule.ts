/**
 * Empty module shim for Node.js built-ins that are transitively imported
 * by engine dependencies (e.g., @ar.io/wayfinder-core → crypto)
 * but not actually used in the browser.
 */
export default {};
